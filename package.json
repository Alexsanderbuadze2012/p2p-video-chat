echo '{
  "name": "P2P Video call",
  "version": "1.0.0",
  "main": "server.js",
  "dependencies": {
    "ws": "^8.5.0"
  }
}' > package.json
git add package.json
git commit -m "Add package.json"
git push origin main
